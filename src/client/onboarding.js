window.setup = async function () {
    createCanvas(windowWidth, windowHeight);
    background(0);
    fill(255);
    textSize(32);
    textAlign(CENTER, CENTER);
    text("THIS IS ONBOARDING.JS!", width / 2, height / 2);
}